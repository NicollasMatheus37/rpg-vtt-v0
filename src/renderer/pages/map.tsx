import React from "react";

export default function Map() {
	return (
		<div style={{ padding: 24, fontFamily: "sans-serif" }}>
			<h1>🗺️ Map Page</h1>
			<p>This is the map page of the Electron + React + TypeScript application.</p>
		</div>
	);
}